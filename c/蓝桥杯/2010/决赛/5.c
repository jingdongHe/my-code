#include<stdio.h>
//m: 持有5角币的人数
//n: 持有1元币的人数
//返回：所有顺利完成购票过程的购票次序的种类数
//n个5角，m个1元，求花费0.5*(m+n)的办法
int main()
{
		int n,m;
		scanf("%d%d",&n,&m);
        if(m < n) return 0;
        if(n==0) return 1;
        return ___________（5）____________;
}
